import 'package:flutter/material.dart';
class  SavingDepositCard extends StatelessWidget {
  const  SavingDepositCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
       child: Text('储蓄卡'),
    );
  }
}