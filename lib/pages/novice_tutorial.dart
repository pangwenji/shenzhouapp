import 'package:flutter/material.dart';
class  NoViceTutorial extends StatelessWidget {
  const  NoViceTutorial({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
       child: Text('新手教程'),
    );
  }
}