import 'package:flutter/material.dart';
class ConsolidatedGoods extends StatelessWidget {
  const ConsolidatedGoods({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
       child: Text('合并货物'),
    );
  }
}